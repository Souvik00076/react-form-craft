import { FC } from "react";

type TFormCardType = {
  className: string;
  title: string;
  subTitle: string;
  value: number;
};
export const FormCard: FC<TFormCardType> = () => {
  return <div></div>;
};
